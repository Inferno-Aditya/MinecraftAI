# API Provider Configuration
# Set to 'gemini', 'openai', or leave blank/unset to run in local mock mode
AI_PROVIDER=

# API Keys (Provide one of these depending on the AI_PROVIDER selected)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional settings
PORT=8000
HOST=127.0.0.1
